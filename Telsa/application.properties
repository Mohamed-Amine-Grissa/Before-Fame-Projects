# Intentionally left without Spring datasource keys.
#
# Why: a project-root `application.properties` is treated as external config
# and overrides `src/main/resources/application.properties` and test configs.
#
# Put runtime config in:
# - src/main/resources/application.properties (app runtime)
# - src/test/resources/application-test.properties (tests)
